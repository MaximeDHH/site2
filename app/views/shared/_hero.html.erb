<section class="hero-contact" style="background-image: url('/assets/hands-rdv.jpg');">
  <div class="hero-contact__content">
    <p class="hero-eyebrow-pill">Contact · Paris</p>
    <h1><%= hero_title %></h1>
    <% if local_assigns[:hero_description] %>
      <p class="hero-sub"><%= hero_description %></p>
    <% end %>
  </div>
</section>
